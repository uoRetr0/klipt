.DS_Store
node_modules
/build
/.svelte-kit
/package

# Bundled FFmpeg sidecars — large binaries, fetched locally, not committed.
# See README / scripts to stage them into src-tauri/binaries/.
/src-tauri/binaries/
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
