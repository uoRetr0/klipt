// Loads jest-dom's matcher augmentation (toBeInTheDocument, toHaveAttribute, …)
// into Vitest's `expect` so both `vitest run` and `svelte-check` recognise the
// DOM matchers used in component tests.
import "@testing-library/jest-dom/vitest";
